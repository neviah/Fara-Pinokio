# Core dependencies
gradio==5.50.0
playwright>=1.40.0
openai>=1.0.0
Pillow>=9.0.0
tenacity>=8.0.0
numpy>=1.20.0

# Optional dependencies for enhanced functionality
# Install torch via torch.js (GPU/CPU conditional)
# transformers optional; leave commented unless required
# transformers>=4.30.0
requests>=2.25.0
aiohttp>=3.8.0

# Development and utility
python-dotenv>=0.19.0
packaging>=21.0